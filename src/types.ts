export type TaskType = {
    ID: string;
    taskName: string;
    taskDesc: string;
};

export type TaskListType = TaskType [];

export type TaskInfoType = {
    taskNameInfo: string;
    taskDescInfo: string;
};